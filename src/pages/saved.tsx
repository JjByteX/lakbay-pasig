// Stub for step 8 (Public app: Saved and Profile, per build-order.md item
// 8). No auth gate on the stub itself, per Phase 2.4.
export default function SavedPage() {
  return (
    <div className="mx-auto flex max-w-md flex-col gap-4 px-6 py-10">
      <h1 className="text-xl font-semibold text-foreground">Saved</h1>
      <p className="text-base text-muted-foreground">Coming in step 8.</p>
    </div>
  );
}
