# architecture-notes.md

---
## AI RULES, READ FULLY BEFORE ANY ACTION
---

1. Read this entire file before proposing any structure, modifying
   any file, or debugging anything.
2. Project scale drives everything. Read the Scale field first.
   Do not propose a structure that exceeds what the scale requires.
3. Before touching any file, identify all files connected to it.
   This map is your reference. If a connection is not listed here,
   find it before proceeding. Do not assume it is isolated.
4. If the structure evolves during the build → update this file.
   An outdated map is worse than no map.
5. Never create folders or layers that the current scale does not
   justify. Do not over-architect small projects. Do not under-
   structure large ones.

---
## SCALE, READ THIS FIRST
---

**Project Scale:**
[x] Medium, multiple features, growing complexity, needs modularity

**Scale Guidelines the AI must follow:**

Small → near-flat structure. Minimal subfolders. Group only what
        genuinely needs grouping. No layers for their own sake.

Medium → feature-based grouping. Each feature owns its files.
         Shared utilities in one place. Clear separation of concerns
         without over-engineering.

Large → fully modular. Domain-driven. Each module is independently
        navigable. Explicit dependency boundaries. Documented entry
        points for every major section.

---
## PROJECT REFERENCE
---

**Project Type:**
Progressive Web App

**Tech Stack:**
- Frontend: React with TypeScript, built with Vite
- UI Components: shadcn/ui with Tailwind CSS
- Backend and Database: Supabase, built on PostgreSQL
- Auth: Supabase Auth, email verification only
- Hosting: Hostinger Node.js hosting, deployed through GitHub integration, connected to Supabase through Hostinger's built in database connector
- Other: role based access through Supabase row level security, matching the Guest, Registered User, Vendor, and CATO Staff roles defined in navigation-and-access-control.md and admin-panel-spec.md

Reasoning: the data model is relational, Places, Trails, Businesses, and Staff permissions all link to each other. PostgreSQL fits this better than a NoSQL database. Supabase adds row level security that maps directly onto the four user roles already defined. shadcn/ui requires TypeScript in its standard setup path, which also adds type safety across the connected data model for a team of three.

**Entry Points:**
What are the main files or modules everything else flows from?
(e.g. main.js, App.tsx, index.py, routes/index.ts)

**Folder Structure:**
Paste or describe the current structure here.
AI: if this is empty at project start → propose a structure based
on the Scale field above, explain each folder's purpose, and wait
for human approval before creating anything.

**Module / Feature Map:**
List the main modules or features and what each one is responsible for.
- Home → recently verified content, CATO announcements, program updates
- Discover → search and map lookup for places and businesses, verification labels shown on every result
- Trails → trail catalog, trail detail with sequenced stops, Discovery content unlock, trail completion and credentials
- Saved → saved places, saved trails, completed trails, earned credentials
- Profile → account info, preferences, vendor mode toggle entry point
- Vendor Mode → business listing creation, listing tiers (Basic, Featured), item and price management, vendor dashboard metrics
- CATO Admin Panel → sidebar sections for Places, Businesses, Events and Announcements, Trails, Staff, each gated by Staff or Admin role
- Auth → Supabase Auth, email verification, role assignment for Guest, Registered User, Vendor, CATO Staff

**Key Dependencies Between Files:**
Which files depend on which? What breaks if X changes?
(This is the map the AI uses before touching anything)
- src/lib/auth-context.tsx → depends on → src/lib/auth-types.ts, supabase/migrations/0001, 0002 (profiles columns)
- Any future admin panel Places section → depends on → places, place_photos, place_reviews (0003)
- Any future admin panel Businesses section → depends on → businesses, business_items, business_reviews, business_flags (0004), business_photos and the profiles_select_staff_business_submitters policy (0008)
- Any future admin panel Trails section → depends on → routes, route_stops, discovery_content (0005)
- Any future admin panel Events section → depends on → events (0006)
- Public Trails tab, Saved tab → depends on → trail_credentials, user_credentials, completed_routes, saved_places, saved_routes (0007)
- route_stops.stop_id and discovery_content.related_location_id → depends on → places.id or businesses.id, no foreign key enforces this, app layer must guarantee stop_type/related_location_type matches a real row

**Database Schema (Build 2, Core Data Models):**

Migrations 0001 to 0007, in `supabase/migrations/`. All tables have row level security enabled, no table grants unauthenticated write access.

| Migration | Tables | Notes |
|---|---|---|
| 0001 | profiles | Base auth extension. role (resident, staff), staff_role (staff, admin) |
| 0002 | profiles (extended) | Adds End User fields and staff fields (position, system_permission) |
| 0003 | places, place_photos, place_reviews | Verification status pending/verified/rejected, review log required per admin-panel-spec.md |
| 0004 | businesses, business_items, business_reviews, business_flags | business_items replaces the single Price Range field. business_flags covers both abuse flags and user reports |
| 0005 | routes, route_stops, discovery_content | route_stops and discovery_content use a type-plus-id pattern to reference either a place or a business, no foreign key on that column, app layer must enforce it points at a real row |
| 0006 | events | published (draft/live) kept separate from lifecycle_status (upcoming/ongoing/past) |
| 0007 | trail_credentials, user_credentials, completed_routes, saved_places, saved_routes | user_credentials and completed_routes are cohort-stat sources only, never queried as a per-user leaderboard, per competitive-positioning.md |
| 0008 | business_photos (new table); profiles (new select policy only) | Build 3, phase 6.1 follow-up. Adds the photo storage 0004 left out for Businesses, and a narrow profiles read policy scoped to accounts that submitted a business, so a review_businesses reviewer can read created_at. Both back the "no photos" and "new accounts" signals in src/lib/business-queue-priority.ts |

RLS pattern used throughout: a `_select_public` policy with no `to` clause (defaults to public, covers Guest with no sign-in) gated on a status column (verified, published, active), a `_select_staff` and `_write_staff` pair checking `staff_role = 'admin' or '<permission>' = any(system_permission)`, and for owner-writable tables (businesses, saved_*, user_credentials, completed_routes) a `using (auth.uid() = <owner column>)` policy. Column-level protection (e.g. a vendor must not send verification_status in their own update) is not enforced by RLS, same limitation documented in migration 0001, the app layer must not send staff-only fields in a self-update request.

Fields present in docs/data-model.md but intentionally left out of the schema, since they are derivable from a join table instead of stored directly: places.Trails Included In, businesses.Trails Included In, routes.Places Included and Place Order (both live in route_stops), trail_credentials.Users Earned. Recomputing these from route_stops and user_credentials avoids duplicated data going stale.

**What Must Never Be Touched Without Human Approval:**
Database schema (supabase/migrations/*.sql), auth logic (src/lib/auth-context.tsx, auth-types.ts), RLS policies.

**Known Fragile Areas:**
- route_stops.stop_id and discovery_content.related_location_id have no foreign key, since they point at either places or businesses. A bad stop_type value or an id that doesn't exist in the matching table will not be caught by the database.
- RLS update policies guard rows, not columns. Any form that lets a vendor edit their own business, or a user edit their own profile, must not submit staff-only or role fields, the database will not block it if the row is otherwise theirs.

---
## STRUCTURE CHANGE LOG
---
AI: when the structure changes during the build, log it here.

| Date | Change | Reason |
|------|--------|--------|
| Build 2 | Added profiles.position, profiles.system_permission | Needed before any staff-gated table could check permissions |
| Build 2 | Added routes.status (draft/published) | Not in data-model.md, needed since admin-panel-spec.md implies a build-then-publish flow with no second reviewer |
| Build 2 | Added events.published, separate from lifecycle_status | admin-panel-spec.md names create/edit/publish as distinct actions, implying a draft state distinct from Upcoming/Ongoing/Past |
| Build 2 | Added discovery_content.needs_place_review | Flags the admin-panel-spec.md exception where new historical claims in Discovery content must route through the Places review queue |
| Build 3, phase 6.1 | Added business_photos table (0008) | data-model.md's "Pictures" field for Local Business was never given a table in 0004, unlike place_photos in 0003. Needed to evaluate the "no photos" review queue signal from vendor-mode-spec.md |
| Build 3, phase 6.1 | Added profiles_select_staff_business_submitters policy (0008) | profiles_select_admin (0001) only covered Admin. A Staff reviewer with only review_businesses had no way to read a vendor's created_at, needed for the "new account" review queue signal from vendor-mode-spec.md. Scoped to profiles that submitted a business, not profiles generally |
| Post-build-3 fix, 0009 | Replaced profiles_select_admin and profiles_update_admin with a public.is_admin() security definer function, same policies now call it instead of re-querying profiles inline | profiles_select_admin's own subquery selected from public.profiles, the same RLS-protected table the outer policy gates. This worked while every account's staff_role was null, since only profiles_select_own matched, but once an account's staff_role was set to admin, the self-referencing subquery caused the login profile read to hang. is_admin() runs as security definer, resolving the staff_role check without re-entering RLS on profiles |
| Phase 5.3 | Added src/pages/admin-discovery-content-review.tsx and the /admin/places/discovery/:id route (App.tsx), gated on manage_places | step-4-plan.md's review exception reuses "the same table and verify/reject actions" for flagged discovery_content rows, but admin-place-detail.tsx is specifically the Place edit form, none of whose fields apply to a discovery_content row. A small dedicated review view was added instead of stretching that form to a second shape, per constraints.md's Inventory Before Suggesting rule against forcing an unrelated fit. admin-places.tsx now also queries discovery_content (needs_place_review = true) alongside places and merges both into one list with a Type column, per step-4-phases.md 5.3 |
| Phase 5.3 fix | Reject on a discovery_content row is log-only, no longer sets discovery_content.status = 'inactive' | An earlier pass had Reject auto-unpublish the row, which silently pulls a stop's content out of a live sequence. competitive-positioning.md and build-priorities.md both define a heritage walk as a narrative arc, each stop referencing the last and setting up the next, not standalone trivia — auto-removing one stop's beat breaks that arc with no review UI surfacing the sequence impact. admin-panel-spec.md's Trail Publishing exception gates PUBLISH ("route it through the Places review queue before the Trail goes live"), it doesn't police content already live. The actual fix now lives at the publish gate (5.4) instead |
| Phase 5.4 (partial) | admin-trails.tsx's publish gate now also blocks on any linked discovery_content with needs_place_review = true, not just stop_count = 0; blocked message links to the flagged entry's review page | admin-panel-spec.md's Trail Publishing exception exists specifically to gate publish on unresolved flagged content — the file's own prior comment already flagged that Phase 5 needed to close this gap "not silently skip it." The in-builder Review and Publish step (step-4-phases.md 5.4's other half — a trail-wide summary of flagged content inside the trail builder itself) is still deferred, this only covers the list-page gate |
| Pre-5.4 fix, human-approved | admin-trail-builder.tsx's persistStops rewritten from delete+reinsert to in-place update for survivors (matched by route_stops.id, `temp-` prefix distinguishes an unsaved pick from a real row) | A prior-pass comment in loadDiscoveryContent had already flagged that delete+reinsert changes route_stops.id on every Stops-step edit, silently nulling discovery_content.related_route_stop_id (on delete set null) for any content already attached. Left unfixed at the time per constraints.md's No Silent Overrides rule, flagged instead of patched. Confirmed still live and fixed now, immediately before 5.4, since 5.4's summary is exactly where an orphaned link would first surface as "missing" content to staff |
| Phase 5.4 (complete) | admin-trail-builder.tsx's Review and Publish step now shows a trail-wide summary (stop count, discovery content count, any flagged entries with the blocking stop named and a link to admin-discovery-content-review.tsx) and a publish/unpublish control | Closes the other half of 5.4 left open by the entry above. Reuses admin-trails.tsx's two blocking conditions (no stops; any needs_place_review = true) rather than inventing new gate logic, per constraints.md's Inventory Before Suggesting rule. Publish button is disabled up front here (not just erroring on click) per ux-ui-guidelines.md's Disabled/gated rule, a stricter application than the list page's dropdown-menu pattern allowed for |
| Phase 6.1 | admin-dashboard.tsx's Places pending count now sums places.verification_status = 'pending' with discovery_content.needs_place_review = true, same merged set admin-places.tsx's queue already lists as one. No Events card added, no separate Trails card added | step-4-phases.md 6.1: "pending counts for Events (if a pending concept applies) and any flagged Trail content in the Places queue count." Events has no pending/review concept — published and lifecycle_status are both fully staff-controlled with no second reviewer, admin-panel-spec.md's same "no second reviewer required" reasoning Trails already relies on — so no card was invented for it; a drafts-count card would misrepresent what Events actually asks staff to act on. Flagged Trail content explicitly folds into the Places number per the spec's own wording, not a new card |
| Found during 6.1, deferred | admin-dashboard.tsx's "Recent activity" feed queries place_reviews with no reviewed_type filter, so since migration 0014 it also pulls Trail Content ("discovery_content") review entries and mislabels every one as "a place" | Pre-existing gap from 0014's schema widen, sitting in code touched while building 6.1, not part of 6.1's own scope. Logged in open-questions.md Section 2 #1, human said fix later rather than now, per constraints.md's No Silent Overrides — flagged, not silently patched or silently left undocumented either |
| Post-6.1 fix, resolved | admin-dashboard.tsx's "Recent activity" feed now queries place_reviews twice, once per reviewed_type ('place', 'discovery_content'), and labels each set correctly ("a place" vs "Trail Content") instead of one unfiltered query | Closes open-questions.md Section 2 #1. No schema change needed, migration 0014 already added reviewed_type; this was a query-shape fix only |
| Read-through fix | admin-events.tsx's row action dropdown now includes Publish/Unpublish and a "Set status" (lifecycle_status) submenu, mirroring admin-trails.tsx's row-action publish/unpublish shape (inline error line, disabled trigger while updating) | step-4-phases.md 2.1/2.3 and step-4-plan.md both call for these "as row actions and inside the detail view" — admin-event-detail.tsx already had both controls, admin-events.tsx's list row only had Edit. Reused the existing row-action pattern per constraints.md's Inventory Before Suggesting rule rather than inventing a new one |
| Read-through fix | admin-event-detail.tsx's Publish button is now disabled with a visible reason (missing fields named inline) when title, description, category, or date_time are blank, matching admin-trail-builder.tsx's Review and Publish gate shape | step-4-phases.md 2.4: "Disabled publish button if required fields are missing, per ux-ui-guidelines.md's Disabled/gated rule." Only title is DB-required (0006); description, category, and date_time were chosen as the remaining gate fields since they're what makes an announcement meaningful once public (data-model.md, Home tab's "what CATO just published" per navigation-and-access-control.md). related_program, location, related_place_id, enrollment_info, and language stay optional, matching data-model.md's own "if applicable" framing. Known gap, flagged not fixed: the gate reads live form state, not the last-saved row, so an edited-but-unsaved field can flip the gate before Save Changes is clicked; out of scope here since no unsaved-changes tracking exists anywhere else in this codebase |
| Read-through fix | admin-trail-builder.tsx's persistStops now re-verifies every newly picked stop's id against places/businesses (verified_status = verified) immediately before inserting into route_stops, rejecting with a named, visible error via the existing stopsError state if any id no longer resolves | step-4-phases.md 4.5: "App layer must confirm the picked id actually exists in the matching table before insert, no foreign key enforces this" (architecture-notes.md's own Known Fragile Areas entry for route_stops.stop_id). place-business-picker.tsx loads its list once on open and can go stale while the picker or builder modal stays open; handlePick previously passed the picker's payload straight through to insert with no re-check. Guard sits in persistStops, the single insert choke point, not duplicated in handlePick, per ponytail's root-cause-over-symptom rule |
| Step 5 (Public app: Discover), complete | Widened businesses_select_public (0015) and business_items_select_public (0016) to verified-or-pending; added the public shell (public-shell.tsx, bottom-nav.tsx) and five bottom-nav routes (Home, Trails, Discover, Saved, Profile) in App.tsx; built the combined Discover data layer (discover-types.ts, discover-query.ts) over places (verified only) and businesses (verified or pending); built the map/list Discover screen (discover-map.tsx, discover-list.tsx, discover.tsx) with search, category filter, and price range filter; built the shared result card (result-card.tsx) and the place/business detail pages (discover-place-detail.tsx, discover-business-detail.tsx); added the save action (save-button.tsx) against saved_places; installed leaflet/react-leaflet (Phase 0.2) with leaflet-icon-fix.ts and the leaflet.css import in main.tsx; closed out loading/empty/error states and a mobile responsive pass across every surface above (Phase 8) | step-5-plan.md and step-5-phases.md in full, Phases 0 through 8.5, each phase's own reasoning already logged individually in the detail table below this one. Recorded here as one row, matching how this log's other step-level entries (e.g. "Phase 5.4 (complete)") summarize a finished milestone rather than duplicating every sub-phase's reasoning a second time. Phase 9.1-9.3's live walkthroughs (guest flow, registered-user save/un-save, verification-label check across states) were explicitly skipped this session, not run and not logged, per standing instruction against manual verification; anyone picking this up should run those three before treating step 5 as verified end to end, not just built |
| Step 5, Phase 2 | Added src/components/public/public-shell.tsx and bottom-nav.tsx, new stub pages trails.tsx, discover.tsx, saved.tsx, profile.tsx, and rewrote home.tsx to render as a shell child instead of owning "/" directly; App.tsx's "/" route now mounts PublicShell with five index/child routes | step-5-phases.md Phase 2, per ux-ui-guidelines.md's Layout Shell Rules (top bar, bottom nav belong in the shell, not the page). home.tsx and the shell both claimed "/", flagged per Phase 2.5 and constraints.md's No Silent Overrides rule rather than resolved silently: shell wins as root since navigation-and-access-control.md defines Home as one of the five bottom-nav tabs, home.tsx's existing login/logout content was kept and now renders as the index child. Top bar search slot (public-shell.tsx's TopBarSlotContext) sits in the shell now even though only Discover fills it this step, since a bar that appeared on one tab today and needed to move into the shell later would violate the same rule it's satisfying now. Only Discover, Trails, Saved, Profile are stubs; Discover itself is also a placeholder here, the real map/list screen starts at Phase 3 |
| Step 5, Phase 3 | Added src/lib/discover-types.ts (DiscoverPlace, DiscoverBusiness, DiscoverResult discriminated union) and src/lib/discover-query.ts (fetchDiscoverResults, sortDiscoverResults) | step-5-phases.md Phase 3, read-side query logic built ahead of any map/list UI so UI bugs aren't confused with query bugs. Queries places (places_select_public, migration 0003, verified only, unchanged) and businesses (businesses_select_public, migration 0015, verified or pending) separately since they're different tables with different RLS-visible statuses, merged into one typed list per step-5-plan.md's Shape section (one combined view, not separate data paths per surface). Summary fields only (name, category, coordinates, verification_status, one-line description), full detail fields are Phase 6's concern. sortDiscoverResults uses haversine straight-line distance, sufficient for a list sort order per step-5-phases.md 3.3, not turn-by-turn routing; results with no recorded coordinates sink to a name-sorted tail rather than being dropped, since the list still shows every result regardless of distance-sort availability. businesses.category has no check constraint at the DB layer (only places.category does, per migrations 0003/0004), noted here for Phase 5.4's category filter rather than acted on now, that phase is what the plan itself scopes the "fixed category lists...from check constraints" filter to. Phase 3.4's manual query check against seed data was not run, per standing instruction against manual verification this session |
| Step 5, Phase 4.1-4.2 | Added src/components/public/discover-map.tsx (MapContainer centered on Pasig, RecenterOnLocation using useMap); wired into src/pages/discover.tsx replacing the Phase 2 placeholder; public-shell.tsx's `<main>` changed from flow layout (pb-16 pt-14) to a fixed, sized region (fixed inset-x-0 bottom-16 top-14 overflow-y-auto) so a full-bleed map has a concrete height to fill | step-5-phases.md Phase 4.1 (map container, default Pasig center, full-bleed under the top bar, no card wrapper per ux-ui-guidelines.md, a map is not card content) and 4.2 (browser geolocation requested on mount, recenters via Leaflet's imperative setView through useMap since MapContainer's center prop only applies on first mount; denied/unavailable/errored cases all silently keep the Pasig default, no error state, since the plan frames geolocation here as a fallback not a required permission). Pasig center (14.5764, 121.0851) sourced from published city-center coordinates, no exact site (e.g. City Hall) specified by any doc, general city center is the plan's own wording ("centered on Pasig"). Shell's `<main>` height change checked against the four Phase 2 stub pages (home, trails, saved, profile), none set their own height class, so overflow-y-auto preserves their existing scroll behavior unchanged. Markers (4.3), the map/list toggle (5.1), and search (5.3) are explicitly out of scope for this sub-phase |
| Step 5, Phase 4.3-4.4 | Added src/components/public/result-card.tsx (ResultCard, a Dialog-based modal showing name, category, description, and a verification badge); discover-map.tsx now fetches Phase 3's combined result set, renders one L.divIcon Marker per result with coordinates (two treatments: primary-filled dot for verified, muted-foreground dot for pending business), and opens ResultCard on marker tap | step-5-phases.md 4.3 (marker per place/business from the Phase 3 query, two treatments minimum so the pending visual distinction is anchored in the marker itself, not just the detail card later) and 4.4 (marker tap opens "the same result card component Phase 6 defines," pulled forward here since 4.4 depends on it existing). result-card.tsx is intentionally scoped to only what 4.4 needs: name, category, description, and the verification label ("Verified by Pasig Tourism Office" / "Pending Verification", per navigation-and-access-control.md's v1 scope and vendor-mode-spec.md's badge-distinction requirement). Badge variant mapping reuses admin-places.tsx's STATUS_VARIANT convention (verified: default, pending: outline) rather than inventing new tokens, per constraints.md's Inventory Before Suggesting rule. Full record fields (6.3-6.4), the result-detail route (6.2), and the save action (6.5-6.6) remain Phase 6's own scope, not built here. Marker dots use L.divIcon with Tailwind utility classes (bg-primary, bg-muted-foreground, border-card) instead of new raster icon assets, resolves correctly since index.css already loads Tailwind's utilities globally; native/existing-token approach over adding image files, per ponytail's ladder. Results with no latitude/longitude on record are filtered out of the marker layer (nothing to plot), same rows still appear in Phase 5's list view regardless. Query failure on the combined fetch leaves results empty for now, full error-state handling is Phase 8.3 |
| Step 5, Phase 5 | discover.tsx now owns the combined query, geolocation request, and userLocation state (moved up from discover-map.tsx, which becomes a controlled component taking `results`/`userLocation` props); added src/components/public/discover-list.tsx (DiscoverList) and a map/list Tabs toggle in discover.tsx (Phase 5.1); search input filled into public-shell.tsx's existing top bar slot via useTopBarSlot (5.3) and a category Select (5.4) both narrow one filterDiscoverResults result in discover-query.ts, feeding both surfaces per step-5-plan.md's "narrows both map markers and the list together"; result-card.tsx's VerificationBadge exported for reuse by list rows (5.2's "same card whether reached from map or list") | step-5-phases.md Phase 5 in full (5.1 list toggle, local state, no second tab/route; 5.2 list rendering, same result set and card component, sorted via Phase 3.3's sortDiscoverResults; 5.3 search wired to the top bar slot, filters both surfaces in place, no separate search page; 5.4 category filter from a fixed list). State was lifted out of discover-map.tsx rather than duplicated into a second fetch/geolocation call in discover-list.tsx, since both surfaces need the identical filtered set and the same user location, per constraints.md's Inventory Before Suggesting rule and ponytail's root-cause-over-symptom rule (two independent fetches would double the query and could desync). Category filter's fixed option list is DISCOVER_CATEGORIES (discover-types.ts), places.category's five check-constraint values only, matching admin-place-detail.tsx's own CATEGORIES const; businesses.category has no check constraint (flagged in the Phase 3 row above), so the filter still applies to businesses by exact string match on whatever free-text value they carry, rather than inventing a second fixed list not backed by any constraint. Map/list toggle reuses the existing Tabs primitive as a segmented control instead of adding a new toggle-group dependency, per ponytail's ladder (already-installed primitive over a new one); TabsContent intentionally unused so the map instance stays mounted across a toggle rather than remounting on every switch. Search input styling (icon-in-input, `pl-9`) matches the existing pattern already in place-business-picker.tsx rather than inventing a new one, per ux-ui-guidelines.md's Consistency Rules. Empty-result state added inline in discover-list.tsx (Phase 8.2 still owns the full cross-cutting states pass); no npm install run this phase, no new dependency was needed, everything used (Tabs, Select, Input, lucide-react icons) was already installed. No manual verification run, per standing instruction this session |
| Step 5, Phase 6, read-through fix | result-card.tsx's modal now includes a "View full details" link (Button `asChild` wrapping a react-router-dom `Link`, `variant="link"`) in a `DialogFooter`, routing to `/discover/${result.kind}/${result.id}` and closing the modal on click | discover-place-detail.tsx and discover-business-detail.tsx (both already present in this pass, Phase 6.2-6.4) and App.tsx's `discover/place/:id` / `discover/business/:id` routes existed with no way to reach them: result-card.tsx imported `Link` and `DialogFooter` but never used either, per constraints.md's Debugging Protocol the root cause was traced to this file, not the detail pages themselves. Closes the loop step-5-phases.md 6.1/6.2 describe ("opens from a marker tap or list row tap" reading through to the full detail page), matching the pattern already used for row-to-detail navigation elsewhere in this codebase (e.g. admin-trails.tsx's Link-based rows) rather than inventing a new navigation affordance, per ponytail's reuse-before-write rung. `variant="link"` chosen since this is a secondary action inside a preview modal, not competing with the modal's own close action |
| Step 5, Phase 6, schema fix, human-confirmed | New migration `supabase/migrations/0016_widen_business_items_select_public.sql`: `business_items_select_public` widened from checking the parent business's `verification_status = 'verified'` to `verification_status in ('verified', 'pending')`, exists check against `businesses`, same shape 0015 already established for the parent table | Found while building discover-business-detail.tsx's item list (Phase 6.4): migration 0015 widened `businesses_select_public` but never touched `business_items_select_public`, so a pending business's item list silently returned zero rows under RLS, not an error, for the exact case Phase 1 widened visibility to cover. step-5-phases.md 6.4 explicitly scopes the item list through "the widened businesses_select_public policy from Phase 1," i.e. matching the parent record's visibility, which this migration now actually delivers. Schema and RLS are on the never-touch-without-approval list, flagged and confirmed before writing per constraints.md's No Silent Overrides rule, same as Phase 1's own migration. `rejected` and `unverified` stay excluded, matching the parent policy exactly, no new reasoning needed beyond 0015's |
| Step 5, Phase 7 | discover-types.ts's DiscoverBusiness gained `itemPrices: (number \| null)[]`; discover-query.ts's fetchBusinesses now embeds `business_items(price)` in the same query (Supabase foreign-table select, same syntax already used elsewhere in this codebase, e.g. admin-dashboard.tsx's `profiles(display_name)`); filterDiscoverResults gained an optional fourth `priceRange` parameter; discover.tsx added minPrice/maxPrice string state, a derived `priceRange` memo, and a second filter row (two number Inputs with Labels) beneath the existing toggle/category row | step-5-phases.md Phase 7 in full (7.1 price range control, narrows the combined set to businesses with at least one item in range, per step-5-plan.md section 3; 7.2 an item with no price is skipped by this filter only, never hidden from the base list or from Discover generally, per vendor-mode-spec.md's Filter Behavior line — implemented as `price != null` inside the `.some()` match, a business with some priced and some unpriced items still appears if one priced item matches). A place is excluded whenever the filter is active, per step-5-plan.md scoping this filter to businesses; DiscoverPlace has no itemPrices field so this falls out of the type shape itself rather than a special-cased branch. Two plain number Inputs (min/max) instead of a Slider, no Slider component exists in this codebase (checked components/ui/ per constraints.md's Inventory Before Suggesting rule) and price has no fixed band list to pick from, unlike category's check-constraint values, so a min/max pair is the native way to express an open-ended range, per ponytail's native-platform-feature rung. Own row beneath the toggle/category row rather than packed into it, the max-w-md mobile container (public-shell.tsx) has no room for a third control inline without shrinking the existing two below a usable tap target, per ux-ui-guidelines.md's Responsive Rules and Component Sizing Rules. Raw input values kept as strings in state and parsed to numbers only inside the memo, so an in-progress or empty edit doesn't force a premature filter value; both empty maps to `null` (filterDiscoverResults' existing no-op case), matching how query/category already behave when unset. No npm install run this phase, no new dependency needed. No manual verification run, per standing instruction this session |
| Step 5, Phase 8.1 | discover.tsx added `resultsLoading` state around the existing combined fetch, passed down as a new required prop to both discover-map.tsx and discover-list.tsx; discover-list.tsx renders three row-shaped `Skeleton` placeholders (components/ui/skeleton.tsx, previously unused anywhere in this codebase) while `resultsLoading` is true, ahead of its existing zero-results empty-state branch; discover-map.tsx gained a local `TileLoadIndicator` helper (reads the underlying Leaflet map's core `loading`/`load` events via `useMap`, the same way `RecenterOnLocation` already reads the instance in this file) tracking `tilesLoading`, and a small pill positioned over the top of the map (`absolute`, `z-[1000]` to clear Leaflet's own pane stack) shown whenever `resultsLoading` or `tilesLoading` is true, wrapping `MapContainer` in a new `relative h-full w-full` div to anchor it | step-5-phases.md 8.1: "Map tiles loading, result query in flight, detail page loading, distinguished from empty states, not a single shared spinner meaning three different things." Detail-page loading (discover-place-detail.tsx, discover-business-detail.tsx) was checked and already satisfies this: both gate their `loading` and `notFound` branches on the same boolean so they never render together, no change needed there, confirmed by reading both files rather than assumed. The other two named cases (map tiles, result query) had no loading state at all before this phase — discover.tsx's fetch effect previously set `results` directly with no in-flight flag, and discover-map.tsx's `TileLayer` gave no feedback while raster tiles loaded, both were a blank/empty-looking surface indistinguishable from a genuine zero-result response, which is the exact "shared spinner meaning three different things" failure this sub-phase exists to close. Result-query loading and tile loading are two independent signals (a slow tile server and a slow Supabase query are unrelated causes) rather than one merged boolean, so the map's pill message differs depending on which is still true. Skeleton reused as-is from the existing primitive rather than a new spinner dependency, per ponytail's native-feature-first rung and constraints.md's Inventory Before Suggesting rule. Query-failure handling and the zero-result empty-state copy/styling are both explicitly out of scope here, per the plan's own split (8.2, 8.3) and the inline comments already in discover.tsx and discover-list.tsx scoping them there; discover.tsx's fetch `.catch()` still swallows errors into an empty result unchanged. No npm install run, no new dependency added. No manual verification run, per standing instruction this session |
| Step 5, Phase 8.2 | discover-map.tsx gained a derived `isEmpty` flag (`!resultsLoading && !tilesLoading && results.length === 0`) and a second top-of-map pill, shown only once both Phase 8.1 loading signals have resolved and the filtered set is still zero, reading the identical copy discover-list.tsx's existing empty-state branch already used ("No results match your search and filters.") | step-5-phases.md 8.2: "No results for the current search or filter combination. One clear, specific label, not a repeated restatement, per ux-ui-guidelines.md's Label Rules." discover-list.tsx already had this exact case handled correctly since Phase 5 (confirmed by reading it, comment there updated to reflect the confirmation rather than deferring further); discover-map.tsx had no equivalent at all, an empty filtered set previously rendered as a bare map with zero markers and no message, indistinguishable at a glance from "still loading" before Phase 8.1's pill existed and, even after it, from "there was never anything to search" once loading resolved. Copy is byte-identical between the two surfaces rather than a second phrasing of the same idea, per Label Rules' "never use synonyms for the same concept across the same screen" and since both surfaces read the same filtered set (step-5-plan.md's "narrows both map markers and the list together"), they should never disagree about whether a given search/filter combination has results. Reuses Phase 8.1's existing pill slot/z-index instead of a second overlay mechanism, per constraints.md's Inventory Before Suggesting rule; map stays interactive (pan/zoom) under the empty pill rather than the list's full-body swap, since a person may still want to look around manually with zero pinned results, matching navigation-and-access-control.md's "search and view" framing for this screen. discover-business-detail.tsx's item list (renders nothing, no message, when a business has zero items) was checked and deliberately left as-is: it already matches the pattern every other optional field on both detail pages follows (omit the section rather than announce absence), and 8.2's own scope line is specifically about the Discover search/filter result set, not every possibly-empty field on a detail page; treated as out of scope rather than silently expanded into. No npm install run, no new dependency added. No manual verification run, per standing instruction this session |
| Step 5, Phase 8.3 | discover.tsx's fetch effect now sets a `resultsError` string (or null) alongside `resultsLoading`, extracting `.message` from Supabase's thrown PostgrestError (a plain object, checked via property narrowing rather than `instanceof Error`, since PostgrestError is not an Error subclass) with a named fallback sentence for any other shape; passed to both discover-map.tsx and discover-list.tsx as a new required prop. discover-list.tsx checks `resultsError` first, ahead of its loading and empty branches, rendering the message in `text-destructive`. discover-map.tsx checks it first too: a new error pill (same shape as the loading/empty pills, `text-destructive` on the message only) replaces the loading/empty pills entirely while an error is present, and `isEmpty` now excludes the error case so the two never compete. save-button.tsx also gained an `error` state: the optimistic save/unsave flip now reports a specific, direction-aware message ("Couldn't save this place. Try again." / "Couldn't remove this place. Try again.") beneath the heart icon if `toggleSavedPlace` fails, instead of silently reverting with no explanation | step-5-phases.md 8.3: "Query failure, geolocation failure beyond the silent-fallback case in Phase 4.2 (for example a network error on the places/businesses fetch itself), specific messages, not 'something went wrong,' per ux-ui-guidelines.md." Geolocation itself was checked and confirmed to need no change: `getCurrentPosition`'s error callback already covers every `GeolocationPositionError` case (denied, unavailable, timeout) in the one existing silent no-op, Phase 4.2's own approved design (cited in that callback's comment); there is no failure mode "beyond" what it already handles, so 8.3's geolocation clause reads as confirming that design rather than asking for new code, and the phase's own parenthetical example points at the fetch as the actual target of this sub-phase. Error checked ahead of loading/empty in both discover-list.tsx and discover-map.tsx, per ux-ui-guidelines.md's State Rules, a failed fetch is a more severe and more specific condition than "still waiting" or "completed with nothing to show" and must never be reported as either. `text-destructive` reused as-is from this codebase's one existing error-message convention (e.g. admin-staff-detail.tsx's notFound branch) rather than a new color or component invented for this screen, per constraints.md's Inventory Before Suggesting rule; no retry button added despite Discover being a primary landing surface unlike the admin pages that convention comes from, since no read-query surface anywhere in this codebase has a retry affordance to match against (checked, none found) and introducing a new interaction pattern under an "error states" phase whose own scope line only asks for specific messages would be scope creep beyond what was asked, left as a flagged option for a human to decide on deliberately rather than added silently. save-button.tsx's fix goes beyond the two cases 8.3 names explicitly (query, geolocation): judged in-scope anyway under Phase 8's own opening line, "every interactive element needs its full state set... checked once across the whole screen," since the save action is part of this step's Discover build and its prior silent revert-with-no-message was a direct violation of State Rules' "Error / failure (with a specific message — not 'something went wrong')" — worse than a generic message, it had none at all. No npm install run, no new dependency added. No manual verification run, per standing instruction this session |
| Step 5, Phase 8.4 | discover.tsx's category Select changed from a fixed `w-40` trigger to `min-w-0 flex-1 [&>span]:line-clamp-1`, its Tabs sibling gained `shrink-0`; discover-business-detail.tsx's item-list row (name span) changed from unconstrained to `min-w-0 flex-1 truncate`, its price span gained `shrink-0` and the row gained `gap-3`; discover-map.tsx's three status pills (loading, empty, error) each gained a `px-6` wrapping gutter and `max-w-full break-words text-center` on the pill itself | step-5-phases.md 8.4: "Confirm the shell, map, list, and detail page all hold up at mobile widths first... then check they do not break wider, per ux-ui-guidelines.md's Responsive Rules." A confirm-and-check pass across every file this step touched, not a rebuild: public-shell.tsx, bottom-nav.tsx (fixed positioning, shared max-w-md, matching h-16/bottom-16 and h-14/top-14 heights, verified by reading both rather than assumed), discover-list.tsx's three message states, discover-place-detail.tsx's back/save row and facilities chip wrap, and result-card.tsx/dialog.tsx (shared DialogContent, `w-full max-w-lg`, identical pattern already used by every other Dialog in this codebase including every admin page, left untouched, out of scope for a Discover-only pass and not a gap Discover introduced) were all read and confirmed already correct at both ends, no change needed. Two real overflow bugs were found and fixed: (1) the category Select's fixed `w-40` (160px) next to the Tabs toggle's own intrinsic width exceeded the row's available content width at a 320px viewport (still a real device width, e.g. older/smaller iPhones) since flex items default to `min-width: auto` and refuse to shrink inside a `justify-between` row with no wrap; fixed by letting the Select flex and shrink instead of holding a fixed pixel width, with `[&>span]:line-clamp-1` (the same child-selector truncation approach shadcn's own SelectTrigger uses upstream) added locally so the longest category options ("Heritage Site", "Cultural Site") ellipsis instead of wrapping or overflowing past the chevron once the trigger is this narrow; applied at the call site rather than editing the shared components/ui/select.tsx primitive, since that file backs every Select in the app. (2) discover-business-detail.tsx's item-list row had no min-w-0/truncate on either span; item names are vendor-submitted free text (vendor-mode-spec.md) with no length bound, so a long name next to the price could overflow the same way; fixed with the name eliding and the price (short, fixed-format, more important to always read in full) staying fixed-width. A third, lower-severity gap was fixed defensively: discover-map.tsx's three status pills had no width constraint on their `rounded-full` span, which is fine for the two fixed-copy messages (loading, empty) but risky for the error pill specifically, since resultsError's length is determined by whatever Supabase or the network actually reports, not authored copy; `max-w-full break-words` lets a long message wrap onto a second line inside the pill rather than stretch past the viewport, applied to all three pills for consistency rather than special-casing just the error one. No npm install run, no new dependency added. No manual verification run, per standing instruction this session |
| Step 5, Phase 8.5 | discover.tsx's map/list Tabs toggle: both TabsTrigger `gap-1.5` changed to `gap-1` | step-5-phases.md 8.5: "Confirm colors, spacing, radius, and font sizes on every new component in this step reference existing tokens, no raw values, no new one-off values invented for Discover specifically, per ux-ui-guidelines.md's Consistency Rules and Design Tokens section." Full audit, not a sample check: every color class, radius class, spacing class, and font-size class across discover.tsx, discover-map.tsx, discover-list.tsx, result-card.tsx, save-button.tsx, public-shell.tsx, bottom-nav.tsx, discover-place-detail.tsx, and discover-business-detail.tsx was enumerated and checked against theme-reference.css/index.css/tailwind.config.ts and against existing usage elsewhere in the codebase (grep across src/pages and src/components/ui, not assumed). Colors: no raw hex anywhere, every class resolves to a CSS variable already defined in both `:root` and `.dark` (checked bg-background, bg-card, bg-muted/-foreground, bg-primary, border-border, border-card, text-destructive, text-foreground, text-muted-foreground, text-primary individually), no dark-mode gaps. Font sizes: text-xs/sm/base/xl (four sizes) are used, but this exact set (plus lg/2xl in two shadcn primitives and admin-dashboard.tsx's stat cards) is already the whole app's existing type scale, confirmed by grepping every page file, not a Discover-specific fourth size invented here, so ux-ui-guidelines.md's "no more than 3, a 4th only when genuinely needed" was already settled app-wide before this step, out of scope to relitigate on a Discover-only pass. Radius: only rounded-full (status pills, marker divIcon dots) and the reused Badge component's own rounded-md (a token, `var(--radius) - 2px`); rounded-full is a shape keyword for a circular/pill element, not a magic-number radius, no raw radius value anywhere. Spacing: the great majority (px-6, gap-2, gap-4, gap-6, py-6, etc.) are clean 8px-grid multiples; the non-8-multiple values found (px-3/py-1 on the map's status pills, left-3 on the search-bar icon, top-3 on the pills' positioning, pb-3/py-3) were traced individually and confirmed to originate from values already built into this codebase's shadcn primitives (Input's own base class is literally `px-3 py-1`; Button's `sm` size is `px-3`) or an already-established pattern reused directly (left-3 for icon-in-input matches place-business-picker.tsx's identical construction, cited in this file's own Phase 5 entry), not one-off values invented for Discover. One real gap found and fixed: the Map/List Tabs toggle's two TabsTrigger elements used `gap-1.5` (6px), a value with no precedent anywhere else in the codebase (checked) and matching neither the 8px grid nor the 4px tight-space allowance ux-ui-guidelines.md's Spacing Rules carve out for icon gaps specifically; bottom-nav.tsx's own icon/label stack already uses `gap-1` (4px) for the identical icon-plus-label-gap purpose, so `gap-1.5` was changed to `gap-1` to match rather than leaving a second, uncoordinated value for the same purpose in place, per Consistency Rules' "if a value is used once, it must be reused everywhere that context applies." No npm install run, no new dependency added. No manual verification run, per standing instruction this session |

---
