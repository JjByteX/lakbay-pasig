# architecture-notes.md

---
## AI RULES, READ FULLY BEFORE ANY ACTION
---

1. Read this entire file before proposing any structure, modifying
   any file, or debugging anything.
2. Project scale drives everything. Read the Scale field first.
   Do not propose a structure that exceeds what the scale requires.
3. Before touching any file, identify all files connected to it.
   This map is your reference. If a connection is not listed here,
   find it before proceeding. Do not assume it is isolated.
4. If the structure evolves during the build → update this file.
   An outdated map is worse than no map.
5. Never create folders or layers that the current scale does not
   justify. Do not over-architect small projects. Do not under-
   structure large ones.

---
## SCALE, READ THIS FIRST
---

**Project Scale:**
[x] Medium, multiple features, growing complexity, needs modularity

**Scale Guidelines the AI must follow:**

Small → near-flat structure. Minimal subfolders. Group only what
        genuinely needs grouping. No layers for their own sake.

Medium → feature-based grouping. Each feature owns its files.
         Shared utilities in one place. Clear separation of concerns
         without over-engineering.

Large → fully modular. Domain-driven. Each module is independently
        navigable. Explicit dependency boundaries. Documented entry
        points for every major section.

---
## PROJECT REFERENCE
---

**Project Type:**
Progressive Web App

**Tech Stack:**
- Frontend: React with TypeScript, built with Vite
- UI Components: shadcn/ui with Tailwind CSS
- Backend and Database: Supabase, built on PostgreSQL
- Auth: Supabase Auth, email verification only
- Hosting: Hostinger Node.js hosting, deployed through GitHub integration, connected to Supabase through Hostinger's built in database connector
- Other: role based access through Supabase row level security, matching the Guest, Registered User, Vendor, and CATO Staff roles defined in navigation-and-access-control.md and admin-panel-spec.md

Reasoning: the data model is relational, Places, Trails, Businesses, and Staff permissions all link to each other. PostgreSQL fits this better than a NoSQL database. Supabase adds row level security that maps directly onto the four user roles already defined. shadcn/ui requires TypeScript in its standard setup path, which also adds type safety across the connected data model for a team of three.

**Entry Points:**
What are the main files or modules everything else flows from?
(e.g. main.js, App.tsx, index.py, routes/index.ts)

**Folder Structure:**
Paste or describe the current structure here.
AI: if this is empty at project start → propose a structure based
on the Scale field above, explain each folder's purpose, and wait
for human approval before creating anything.

**Module / Feature Map:**
List the main modules or features and what each one is responsible for.
- Home → recently verified content, CATO announcements, program updates
- Discover → search and map lookup for places and businesses, verification labels shown on every result
- Trails → trail catalog, trail detail with sequenced stops, Discovery content unlock, trail completion and credentials
- Saved → saved places, saved trails, completed trails, earned credentials
- Profile → account info, preferences, vendor mode toggle entry point
- Vendor Mode → business listing creation, listing tiers (Basic, Featured), item and price management, vendor dashboard metrics
- CATO Admin Panel → sidebar sections for Places, Businesses, Events and Announcements, Trails, Staff, each gated by Staff or Admin role
- Auth → Supabase Auth, email verification, role assignment for Guest, Registered User, Vendor, CATO Staff

**Key Dependencies Between Files:**
Which files depend on which? What breaks if X changes?
(This is the map the AI uses before touching anything)
- src/lib/auth-context.tsx → depends on → src/lib/auth-types.ts, supabase/migrations/0001, 0002 (profiles columns)
- Any future admin panel Places section → depends on → places, place_photos, place_reviews (0003)
- Any future admin panel Businesses section → depends on → businesses, business_items, business_reviews, business_flags (0004), business_photos and the profiles_select_staff_business_submitters policy (0008)
- Any future admin panel Trails section → depends on → routes, route_stops, discovery_content (0005)
- Any future admin panel Events section → depends on → events (0006)
- Public Trails tab, Saved tab → depends on → trail_credentials, user_credentials, completed_routes, saved_places, saved_routes (0007), route_progress (0018)
- route_stops.stop_id and discovery_content.related_location_id → depends on → places.id or businesses.id, no foreign key enforces this, app layer must guarantee stop_type/related_location_type matches a real row

**Database Schema (Build 2, Core Data Models):**

Migrations 0001 to 0007, in `supabase/migrations/`. All tables have row level security enabled, no table grants unauthenticated write access.

| Migration | Tables | Notes |
|---|---|---|
| 0001 | profiles | Base auth extension. role (resident, staff), staff_role (staff, admin) |
| 0002 | profiles (extended) | Adds End User fields and staff fields (position, system_permission) |
| 0003 | places, place_photos, place_reviews | Verification status pending/verified/rejected, review log required per admin-panel-spec.md |
| 0004 | businesses, business_items, business_reviews, business_flags | business_items replaces the single Price Range field. business_flags covers abuse flags and user reports |
| 0005 | routes, route_stops, discovery_content | route_stops and discovery_content use a type-plus-id pattern to reference a place or a business. No foreign key on that column, app layer must enforce it points at a real row |
| 0006 | events | published (draft/live) kept separate from lifecycle_status (upcoming/ongoing/past) |
| 0007 | trail_credentials, user_credentials, completed_routes, saved_places, saved_routes | user_credentials and completed_routes are cohort-stat sources only, never a per-user leaderboard, per competitive-positioning.md |
| 0008 | business_photos (new table), profiles (new select policy) | Adds the photo storage 0004 left out for Businesses, and a profiles read policy scoped to accounts that submitted a business, so a review_businesses reviewer can read created_at. Backs the no-photos and new-account signals in src/lib/business-queue-priority.ts |
| 0009 | profiles (function only) | Adds public.is_admin(), a security definer function. profiles_select_admin and profiles_update_admin now call it instead of subquerying profiles inline, removing a self-referencing RLS recursion |
| 0014 | place_reviews | Widens place_id to reviewed_type/reviewed_id, so the same review log table covers both place and discovery_content reviews |
| 0015 | businesses | businesses_select_public widened from verified to verified or pending |
| 0016 | business_items | business_items_select_public widened to match 0015, verified or pending on the parent business |
| 0017 | places, businesses | Adds verified_at, nullable, set only on the transition into verified. Backfilled from updated_at for rows already verified at migration time |
| 0018 | route_progress (new table) | One row per user per route, highest_unlocked_stop_id references route_stops(id). Backs mid-trail resume for the Public Trails tab (step 7), fills the gap step-7-trail-plan.md flagged: completed_routes is write-once-at-finish, nothing tracked in-progress position before this |

RLS pattern used throughout: a `_select_public` policy with no `to` clause (defaults to public, covers Guest with no sign-in) gated on a status column (verified, published, active), a `_select_staff` and `_write_staff` pair checking `staff_role = 'admin' or '<permission>' = any(system_permission)`, and for owner-writable tables (businesses, saved_*, user_credentials, completed_routes, route_progress) a `using (auth.uid() = <owner column>)` policy. Column-level protection (e.g. a vendor must not send verification_status in their own update) is not enforced by RLS, same limitation documented in migration 0001, the app layer must not send staff-only fields in a self-update request.

Fields present in docs/data-model.md but intentionally left out of the schema, since they are derivable from a join table instead of stored directly: places.Trails Included In, businesses.Trails Included In, routes.Places Included and Place Order (both live in route_stops), trail_credentials.Users Earned. Recomputing these from route_stops and user_credentials avoids duplicated data going stale.

**What Must Never Be Touched Without Human Approval:**
Database schema (supabase/migrations/*.sql), auth logic (src/lib/auth-context.tsx, auth-types.ts), RLS policies.

**Known Fragile Areas:**
- route_stops.stop_id and discovery_content.related_location_id have no foreign key, since they point at either places or businesses. A bad stop_type value or an id that doesn't exist in the matching table will not be caught by the database.
- RLS update policies guard rows, not columns. Any form that lets a vendor edit their own business, or a user edit their own profile, must not submit staff-only or role fields, the database will not block it if the row is otherwise theirs.

---
## STRUCTURE CHANGE LOG
---
AI: when the structure changes during the build, log it here.

| Date | Change | Reason |
|------|--------|--------|
| Build 2 | Added profiles.position, profiles.system_permission | Required before any staff-gated table can check permissions |
| Build 2 | Added routes.status (draft/published) | admin-panel-spec.md implies a build-then-publish flow with no second reviewer |
| Build 2 | Added events.published, separate from lifecycle_status | admin-panel-spec.md treats create, edit, and publish as distinct actions, separate from Upcoming/Ongoing/Past |
| Build 2 | Added discovery_content.needs_place_review | Flags new historical claims in Discovery content for the Places review queue, per admin-panel-spec.md |
| Build 3, Phase 6.1 | Added business_photos (0008) | data-model.md's Pictures field for Local Business had no table. Needed for the no-photos review queue signal in vendor-mode-spec.md |
| Build 3, Phase 6.1 | Added profiles_select_staff_business_submitters policy (0008) | A review_businesses staff member had no way to read a vendor's created_at, needed for the new-account review queue signal. Scoped to profiles that submitted a business |
| 0009 | Added public.is_admin(), replaced profiles_select_admin and profiles_update_admin's inline subqueries with a call to it | The inline subquery re-entered RLS on profiles and caused the login read to hang once an account's staff_role was set to admin |
| Phase 5.3 | Added admin-discovery-content-review.tsx and /admin/places/discovery/:id, gated on manage_places | admin-place-detail.tsx is the Place edit form, its fields don't fit a discovery_content row. admin-places.tsx now merges places and flagged discovery_content into one queue with a Type column |
| Phase 5.3 | Reject on a discovery_content row is log-only, no longer sets status to inactive | Auto-unpublishing silently pulled a stop out of a live sequence with no review UI showing the impact. The Trail Publishing exception gates publish, not content already live |
| Phase 5.4 | admin-trails.tsx's publish gate now also blocks on any linked discovery_content with needs_place_review = true, beyond the existing stop_count = 0 check. admin-trail-builder.tsx's Review and Publish step shows a trail-wide summary and a publish/unpublish control | admin-panel-spec.md's Trail Publishing exception gates publish on unresolved flagged content. Publish is disabled up front, per ux-ui-guidelines.md's Disabled/gated rule |
| Pre-5.4 fix | admin-trail-builder.tsx's persistStops rewritten from delete and reinsert to in-place update, matched by route_stops.id | Delete and reinsert changed route_stops.id on every edit, nulling discovery_content.related_route_stop_id for any content already attached |
| Phase 6.1 | admin-dashboard.tsx's Places pending count sums places pending with discovery_content.needs_place_review = true. No Events or Trails card added | Events has no pending concept, both published and lifecycle_status are staff-controlled with no second reviewer. Flagged Trail content folds into the Places number, per step-4-phases.md |
| Post-6.1 fix | admin-dashboard.tsx's Recent activity feed now queries place_reviews once per reviewed_type and labels each set correctly | The prior unfiltered query mislabeled discovery_content reviews as place reviews after migration 0014 widened the table |
| Read-through fix | admin-events.tsx's row action dropdown gained Publish/Unpublish and a Set status submenu, matching admin-trails.tsx's row-action shape | admin-event-detail.tsx already had both controls, the list row only had Edit |
| Read-through fix | admin-event-detail.tsx's Publish button is disabled with an inline reason when title, description, category, or date_time are blank | Matches admin-trail-builder.tsx's gate shape. Known gap: the gate reads live form state, not the last saved row |
| Read-through fix | admin-trail-builder.tsx's persistStops re-verifies each picked stop's id against places or businesses immediately before insert | place-business-picker.tsx's list can go stale while its modal stays open. No foreign key covers route_stops.stop_id |
| Step 5 | Widened businesses_select_public (0015) and business_items_select_public (0016) to verified or pending. Added the public shell and five bottom-nav routes. Built the combined Discover data layer, the map/list screen with search, category, and price filters, the shared result card, and the place/business detail pages. Added the save action. Installed leaflet and react-leaflet | Full step-5-plan.md and step-5-phases.md build, summarized as one row. Manual walkthroughs across guest, save, and verification-label states were not run this session |
| Step 5, Phase 2 | Added public-shell.tsx and bottom-nav.tsx, stub pages for Trails, Discover, Saved, Profile. home.tsx now renders as a shell child instead of owning the root route | ux-ui-guidelines.md's Layout Shell Rules place top bar and bottom nav in the shell, not the page |
| Step 5, Phase 3 | Added discover-types.ts and discover-query.ts | Queries places (verified only) and businesses (verified or pending) separately, merged into one typed list. sortDiscoverResults uses straight-line distance, sufficient for a list sort |
| Step 5, Phase 4.1-4.2 | Added discover-map.tsx, centered on Pasig with geolocation recentering. public-shell.tsx's main region changed to a fixed, sized area so a full-bleed map has a height to fill | Denied or unavailable geolocation keeps the Pasig default, no error state, since it's a fallback not a required permission |
| Step 5, Phase 4.3-4.4 | Added result-card.tsx. discover-map.tsx renders one marker per result with coordinates, two treatments for verified and pending, opening the result card on tap | Marker dots use divIcon with existing Tailwind classes instead of new image assets |
| Step 5, Phase 5 | Moved the combined query, geolocation, and userLocation state up into discover.tsx. Added discover-list.tsx and a map/list Tabs toggle. Search and category filter now narrow one result set feeding both surfaces | Lifting state avoids a second fetch and geolocation call that could desync from the map |
| Step 5, Phase 6 | result-card.tsx's modal now links to the full detail page. Migration 0016 widens business_items_select_public to match 0015 | The link was imported but unused. A pending business's item list returned zero rows under RLS with no widen on the child table |
| Step 5, Phase 7 | Added itemPrices to DiscoverBusiness, a price range filter on discover.tsx narrowing businesses with at least one item in range | An item with no price is skipped by this filter only, never hidden elsewhere, per vendor-mode-spec.md |
| Step 5, Phase 8.1 | Added resultsLoading state, skeleton rows in discover-list.tsx, and a tile-load pill in discover-map.tsx | Map tiles loading, result query in flight, and detail page loading needed to read as distinct from empty, not one shared spinner |
| Step 5, Phase 8.2 | Added an empty-result pill to discover-map.tsx, matching discover-list.tsx's existing copy | An empty filtered set previously showed a bare map with no message |
| Step 5, Phase 8.3 | Added resultsError state, checked first in both discover-list.tsx and discover-map.tsx, text-destructive. save-button.tsx gained a direction-aware error message on save or unsave failure | Query failures need specific messages, not a silent empty result |
| Step 5, Phase 8.4 | Fixed three overflow cases at a 320px viewport: the category Select's fixed width, the business item-list row's untruncated name, and the map's status pills with no width constraint | Confirm-and-check pass across every file this step touched |
| Step 5, Phase 8.5 | Changed the map/list Tabs toggle gap from 6px to 4px, matching bottom-nav.tsx | Token audit across every new Discover file. No other raw value found |
| Post-8.1 fix | discover-map.tsx's tile-load listener now also calls map.whenReady | Leaflet's load event fires once and does not replay to a listener attached after it already fired, which could leave the pill stuck |
| Step 6, Phases 1-5 | Confirmed on read-through: migration 0017 (verified_at, nullable, backfilled from updated_at), admin-place-detail.tsx and admin-business-detail.tsx setting it only on the verify transition, home-types.ts, home-query.ts, announcement-card.tsx, verified-item-card.tsx, event-detail.tsx, and the events/:id route | All present and correct against step-6-phases.md before Phase 6 began |
| Step 6, Phase 6 | home.tsx: added per-section error state set from each fetch's catch, three-row skeleton loading, and a distinct empty-state line per section. Render order is error, then loading, then empty, then loaded | Matches discover-list.tsx's established state order. event-detail.tsx already matched discover-place-detail.tsx's loading/not-found pattern, no change needed |
| Auth visual pass | Added src/assets/ (new folder, first image asset in the repo) and src/components/auth/auth-layout.tsx, a shared split-screen shell (image panel + form panel) wrapping login.tsx and signup.tsx, including signup's "Check your email" success state. login.tsx and signup.tsx's raw `<input>`/`<label>`/`<button>` elements swapped for the existing, previously-unused shadcn Input, Label, and Button components | User supplied a CATO plaza illustration and an inspiration screenshot (split-panel auth screen) and asked for it applied across "the authentication parts," not just login. One shared layout avoids duplicating the panel across both screens. The asset was resized 1671×941→1200×675 and converted PNG→JPEG (~1.9MB→~189KB) since it has no transparency |
| Logo / PWA icons | Added src/assets/lakbay-pasig-logo.svg (squircle-masked logo, self-contained with the source art base64-embedded), and public/ (new top-level folder: favicon.ico, icons/icon-192.png, icons/icon-512.png, icons/maskable-192.png, icons/maskable-512.png, manifest.json). index.html now links favicon, apple-touch-icon, and points rel="manifest" at a file that actually exists. auth-layout.tsx renders the logo above the form panel | index.html referenced /manifest.json since before this session but the file never existed in the repo, a pre-existing gap fixed as part of wiring the logo in. Icon art is padded to ~82% scale inside its square canvas before squircle-clipping so the wordmark clears the corner curvature (verified by rendering the raw superellipse crop first, which clipped into "LAKBAY" at 100% scale). "maskable" manifest entries point at the unclipped square padded art, not the pre-clipped squircle PNGs, since the OS applies its own mask on top and double-clipping would cut the badge a second time — confirmed by simulating a circular Android mask over both variants |
| Step 7, Phase 0 | Added migration 0018 (route_progress: one row per user per route, highest_unlocked_stop_id references route_stops, owner-only RLS matching saved_places_own's shape) | step-7-trail-plan.md's Schema Change First section: completed_routes (0007) is write-once-at-finish, no table tracked mid-trail position, closing the app mid-walk had no way to resume. Human-confirmed per constraints.md, logged as decision-log.md entry #3 |
| Step 7, Phase 0 | Removed leaflet, react-leaflet, @types/leaflet from package.json. Dropped the leaflet.css import and its explanatory comment from main.tsx. Updated discover-map.tsx's own comment referencing react-leaflet from a live scoping note to historical context | map-vector-restyle-plan.md's Open Item ("removing them is a separate, larger decision, out of scope") resolved: grepped, only discover-map.tsx (comment only, no live code) and main.tsx referenced leaflet anywhere in src/, discover-map.tsx has used maplibre-gl exclusively since that plan landed. package-lock.json deliberately left untouched, no npm install run this session per explicit instruction, self-resolves on the next real install |
| Step 7, Phase 1 | Added src/lib/trail-types.ts (TrailSummary, TrailStop, TrailDiscoveryContent, TrailCredential, TrailDetail), src/lib/trail-query.ts (fetchPublishedTrails, fetchTrailDetail), src/lib/trail-progress.ts (getRouteProgress, unlockStop, resetRouteProgress), src/lib/saved-routes.ts (isRouteSaved, toggleSavedRoute) | step-7-trail-plan.md's Data Layer section, no UI yet per constraints.md's automation-before-UI rule. trail-query.ts's stop-name resolve mirrors admin-trail-builder.tsx's loadStops exactly (route_stops.stop_id has no foreign key, migration 0005). saved-routes.ts mirrors saved-places.ts with place_id swapped for route_id. discovery_content fields selected here deliberately exclude needs_place_review, a staff-only routing flag with no use on a public read |
| Step 7, Phase 2 | Added src/components/public/trail-card.tsx (Phase 2.2). trails.tsx (Phase 2.3-2.4, replacing the Phase 5.2.4 stub) and the trails/:id route (Phase 2.1) were already present in this checkout | step-7-phases.md's Phase 2, Trail catalog page. trail-card.tsx's row shows name, theme, duration, and budget on one secondary metadata line, same two-line row shape verified-item-card.tsx and announcement-card.tsx already establish, reused per constraints.md's Inventory Before Suggesting rule. Note: App.tsx already imports src/pages/trail-detail.tsx (the trails/:id route element), which does not exist yet in this checkout — that page is Phase 3's scope, out of scope for this Phase 2 pass, flagged here rather than silently built ahead of the plan per constraints.md's No Silent Overrides rule |
| Step 7, Phase 3 | Added src/pages/trail-detail.tsx, filling the trails/:id route App.tsx already imported since Phase 2. Fetches via fetchTrailDetail (trail-query.ts, Phase 1.3-1.4) keyed by useParams().id, same error/loading/not-found/loaded order and ArrowLeft back button discover-place-detail.tsx and event-detail.tsx already establish | step-7-phases.md's Phase 3, guest preview only, per navigation-and-access-control.md's Guest Trail Preview Decision. Header (3.2) joins theme/duration/budget/run_type on one meta line, same shape trail-card.tsx's row uses; no verification badge, a trail's own publish gate (build_trails, routes_write_staff, 0005) is the trust signal, not a per-record status. Stop list (3.3) is a numbered ol, name only, no lock/unlock state, since progress tracking is Phase 4's scope and trail-stop.tsx does not exist yet. Start button (3.4) only implements the unauthenticated branch (signed-out tap navigates to /login, reusing save-button.tsx's exact pattern, never a disabled control); the signed-in branch is a no-op here on purpose, route_progress creation/resume is Phase 4.3's scope, not built ahead of it per constraints.md's No Silent Overrides rule |
| Step 7, Phase 4.1 | Added src/components/public/trail-stop.tsx (TrailStop component, exported TrailStopState type: "locked" \| "unlocked" \| "completed") | step-7-phases.md's Phase 4.1: one stop's render across its three states, branching inside a single component rather than three, per the plan doc's explicit instruction. Row shape (numbered circle + name) reuses trail-detail.tsx's existing Phase 3.3 `<li>` shape exactly, not reinvented, per constraints.md's Inventory Before Suggesting rule. Locked: name only, muted, Lock icon, no Discovery content rendered. Unlocked and completed both render stop.discoveryContent (title + content) when present, null is a normal valid state per trail-types.ts's own note, not an error; completed additionally shows a CheckCircle2 mark. Lock/CheckCircle2 are both universally recognized icon concepts per ux-ui-guidelines.md's icon rules, no new dependency, lucide-react already installed. This phase builds the component only; trail-detail.tsx still renders its own plain `<li>` list from Phase 3, wiring this component in (and computing which state each stop is in from route_progress) is Phase 4.2-4.3's scope, not built ahead of it per constraints.md's No Silent Overrides rule |
| Step 7, Phase 4.2 | trail-detail.tsx: added a second effect that loads the signed-in user's route_progress row (getRouteProgress, trail-progress.ts, Phase 1.5) once the trail has resolved, storing highestUnlockedStopId in new routeProgress state | step-7-phases.md's Phase 4.2: "load route_progress on mount, signed-in only." No session or no trail yet, resets to null, matching save-button.tsx's own isPlaceSaved effect shape exactly (signed-in-only read, fetch failure falls back to the empty state rather than a second error branch). Data load only, per the plan doc's own phase split: swapping the Phase 3.3 plain `<li>` list for trail-stop.tsx's three-state render, and the Start button's signed-in branch, both stay Phase 4.3's scope, not built ahead of it per constraints.md's No Silent Overrides rule. routeProgress needed a real (not merely declared) consumer to satisfy tsconfig's noUnusedLocals without reaching into 4.3's scope to get one, so the existing stop row gained one inert "Last reached" text marker on the stop matching highestUnlockedStopId — no lock/unlock UI, no trail-stop.tsx wiring, no new interaction, just a truthful reflection of the data this phase loads |
| Step 7, Phase 4.5 | trail-query.ts's TrailStop gained latitude/longitude (resolved in the same places/businesses select as name, no second round trip); trail-detail.tsx added a GPS-watch effect (4.4) and an unlock-check effect (4.5) that compares the watched position against the single next locked stop's coordinate via distanceKm, calling unlockStop (trail-progress.ts) when inside discoveryContent.unlock_radius | step-7-phases.md's Phase 4.5: "for the current locked stop, compare the watched position against that stop's unlock_radius (haversine distance, same distanceKm logic already in discover-query.ts, reused not reinvented)." Only the single next-in-sequence locked stop is checked per tick, never every locked stop at once, matching build-priorities.md's "each stop should reference the last one and set up the next" framing (a trail is a sequence, not a set). A stop with no resolved coordinate or no discoveryContent row is skipped, not guessed in or out of range, same reasoning discover-query.ts's sortDiscoverResults already applies to its own missing-coordinate case |
| Post-Phase-4.5 fix | discover-query.ts's distanceKm changed from a local (unexported) function to `export function distanceKm` | trail-detail.tsx's Phase 4.5 unlock check imports `{ distanceKm }` from discover-query.ts per step-7-phases.md's explicit "reused not reinvented" instruction, but the function was never exported, a build-breaking `TS2459: declares 'distanceKm' locally, but it is not exported` caught by a `tsc --noEmit` pass before hand-off. Root-cause fix at the shared function itself (add `export`), not a second haversine implementation in trail-detail.tsx, per constraints.md's Debugging Protocol ("identify the origin file... not just where the symptom appears") and ponytail's root-cause-not-symptom rule. No other file imports distanceKm, so no other caller is affected |
| Step 7, Phase 4.6 | No code change. Confirm-and-check pass per step-7-phases.md's own 4.6 instruction ("re-confirm 4.6 renders through the normal loaded branch, not through 6.1's error branch or 6.3's empty branch") | Traced every path that can call unlockStop (trail-progress.ts): only trail-detail.tsx's handleStart (Phase 4.3, first stop only, and only when no routeProgress row exists yet) and the proximity effect (Phase 4.5, next locked stop only, GPS-gated). Grepped trail-stop.tsx's locked branch and trail-detail.tsx's stop-list render for any onClick, button, or link on a stop row — none exists, the locked `<li>` (trail-stop.tsx) renders a name and a Lock icon only, matching Phase 4.1's original build. trails.tsx's error/empty divs (Phase 2.4) and trail-detail.tsx's own page-level error/not-found blocks (Phase 3.1) are structurally separate from trail-stop.tsx's per-stop locked branch, so a locked stop was never at risk of rendering through either. Nothing to fix, per-Phase-4.6 requirement was already satisfied by the existing 4.1-4.5 build |
| Step 7, Phase 4.7 | Added src/components/public/save-route-button.tsx (SaveRouteButton), wired into trail-detail.tsx's header in the same flex row as the back button, matching discover-place-detail.tsx's exact SaveButton placement | step-7-phases.md's Phase 4.7: "Reuse the existing heart-icon save-button.tsx pattern, wired to saved-routes.ts (1.6) instead of saved-places.ts, per the plan doc's explicit instruction not to invent a new interaction." save-button.tsx itself is place-only per its own file comment (no saved_businesses table, saved_places has a direct place_id foreign key), so it isn't generalized in place; a sibling component is added instead, matching how saved-routes.ts already sits beside saved-places.ts as a sibling file rather than a shared one. Diffed line-for-line against a mechanical placeId→routeId/saved_places→saved_routes transform of save-button.tsx to confirm the mirror is exact outside of comments. Same signed-out behavior: a signed-out tap navigates to /login, never a disabled heart, enforced independently by saved_routes_own (migration 0007) regardless of this UI check |
| Post-Phase-4.7 fix | discover-map.tsx's LATTE palette object lost its `as const` assertion | CI build failed with 13x `TS2322: Type '"#1e1e2e"' is not assignable to type '"#eff1f5"'` (and one sibling per key), all on MOCHA's declaration (`const MOCHA: typeof LATTE = {...}`). Root cause: `as const` froze every LATTE property to its own exact string-literal type, so `typeof LATTE` demanded MOCHA match LATTE's literal hex values key-for-key instead of just being a string. Neither LATTE nor MOCHA is read anywhere but buildStyle's `palette: typeof LATTE` parameter (grepped both names repo-wide), which only ever consumes them as plain color strings for a MapLibre StyleSpecification, so no caller relied on the narrowed literal type. Root-cause fix at the declaration itself (drop `as const`), not a second color object or a type cast at MOCHA's declaration, per constraints.md's Debugging Protocol and ponytail's root-cause-not-symptom rule |
| Step 7, Phase 5 | Added src/lib/trail-completion.ts (isRouteCompleted, completeTrail), own file matching every other table in this domain having its own lib file, per constraints.md's Inventory Before Suggesting rule (trail-detail.tsx never calls supabase directly anywhere else). trail-detail.tsx: added completed/completing/completeError state (loaded via isRouteCompleted on mount, signed-in only, same shape as the routeProgress load); a shared maybeCompleteTrail(unlockedStopId) helper (5.1) called from both the proximity-unlock effect and handleStart's first-stop branch, covering the one-stop-trail edge case where the first unlock is also the last, guarded on !completed so an already-finished route never double-inserts; completeTrail (5.2) inserts completed_routes then user_credentials if trail.credential exists; the existing "Earn: {name}" badge relabels to "Earned: {name}" once completed (5.3), plus a CheckCircle2 "Trail completed" fallback line for a completed route with no linked credential, and a brief "Saving your completion…" line while completeTrail is in flight (completing needed a real, not merely-declared, consumer per tsconfig.app.json's noUnusedLocals, same reasoning Phase 4.2's own routeProgress fix used); per-stop state now computes "completed" (not just "unlocked") once the whole trail is done; added resetting/resetError state and handleRestart (5.4), which calls resetRouteProgress (already existed, Phase 1.5) and locally sets completed back to false, while deliberately leaving the completed_routes row itself untouched since that table is a completion event log, not current progress — this local-vs-DB divergence is intentional and documented in both trail-detail.tsx and trail-completion.ts's own isRouteCompleted comment. A "Restart trail" button (variant outline, confirmed against button.tsx as a real variant) appears beneath Start/Resume once completed | step-7-phases.md's Phase 5: Completion and credential (5.1-5.4). No schema change needed, completed_routes and user_credentials already existed with owner-writable RLS (migration 0007). Deliberately skipped: a cohort completion-count query/display — the plan doc only requires one "if a completion count is shown at all," it doesn't require adding one in this phase, and no query for one exists yet in trail-completion.ts, per ponytail's YAGNI rung. Follow-up verification (deferred at the end of the prior session, completed now): full top-to-bottom re-read of the finished trail-detail.tsx found no stray references, duplicate logic, or drift between the two maybeCompleteTrail call sites; repo-wide grep confirmed trail-detail.tsx is the only caller anywhere in src/ of isRouteCompleted, completeTrail, resetRouteProgress, and maybeCompleteTrail, and the only file besides trail-completion.ts that references completed_routes or user_credentials at all, so no other page needs the same one-stop-trail or restart-divergence reasoning applied. No bugs found beyond what was already documented inline |
| Step 7, Phase 6.1 | No code change. Confirm-and-check pass per step-7-phases.md's own 6.1 instruction ("Checked first, ahead of loading and empty, on both the catalog and the detail page ... text-destructive, this codebase's one existing error-message convention, no new color invented") | Re-verified trails.tsx's existing ternary chain (built Phase 2.4): error checked first, then loading, then empty, then loaded, text-destructive used on the error line. Re-verified trail-detail.tsx's chain (built Phase 3.1, extended 4.2/5.x): error, then loading, then notFound, then loaded, each gated on the previous being false, same text-destructive convention. Grepped every destructive/error render across every Step 7 file (trails.tsx, trail-detail.tsx, trail-card.tsx, trail-stop.tsx, save-route-button.tsx): completeError, startError, resetError, and save-route-button.tsx's own error all use text-destructive too, no new color invented anywhere. Confirmed these four are a different category from the page-level error/loading/empty triptych (one-shot action feedback on a write, not a fetch state with a loading or empty counterpart to order against), matching home.tsx's own established distinction. Both pages already satisfied 6.1 as originally built, nothing to fix |
| Step 7, Phase 6.2 | Fixed trails.tsx's TrailListSkeleton: dropped from three Skeleton lines per row to two | step-7-phases.md's 6.2: "Row-shaped skeletons on the catalog (matching trail-card.tsx's own row shape...)." The skeleton (built Phase 2.4, before trail-card.tsx's final two-line shape settled) had drifted: it was copied from discover-list.tsx's own 3-Skeleton-line loading state, but that list's real row has three visible elements (name, category, and a VerificationBadge), while trail-card.tsx's real row only ever has two (name, then one metadata line joining theme/duration/budget) since a trail carries no verification badge (Phase 3.2's own reasoning: publication itself is the trust signal). Fixed to two Skeleton lines, gap-1 instead of gap-2 to match trail-card.tsx's own gap-1 exactly. Confirmed the detail page's "Loading…" line (trail-detail.tsx, Phase 3.1) already matches discover-place-detail.tsx's exact text, classes (text-base text-muted-foreground), and gating shape verbatim, no change needed there; the two pages' differing error-vs-notFound split (trail-detail.tsx has a separate error state, discover-place-detail.tsx collapses errors into notFound) is a deliberate, already-documented divergence, not a loading-state mismatch 6.2 is scoped to catch |
| Step 7, Phase 6.3 | No code change. Confirm-and-check pass per step-7-phases.md's own 6.3 instruction ("one clear, specific line, no second phrase restating it ... Distinct wording from the discover and home empty states already in the codebase") | Re-verified trails.tsx's empty state ("No published trails yet.", built Phase 2.4): single line, no restating second phrase, satisfies ux-ui-guidelines.md's Label Rules directly. Grepped every empty-state string across every public and admin page to compare wording: distinct from discover-list.tsx/discover-map.tsx's "No results match your search and filters." and home.tsx's "No announcements yet." / "No recently verified content yet.", the two surfaces the plan doc names explicitly. Also checked against admin-trails.tsx's own "No trails yet." (a different concept, all trails including drafts on a staff-only table, versus trails.tsx's published-only subset on the public catalog, matching the same admin/public wording split already established elsewhere, e.g. admin-places.tsx vs Discover) -- not a Label Rules collision, different concept and different surface. Nothing to fix, 6.3 was already satisfied as originally built. No separate empty state exists on trail-detail.tsx: a trail either exists (loaded) or doesn't (notFound), that page has no zero-items concept of its own to check here; its own "No stops added to this trail yet." line (Phase 2.4) is a sub-section empty state already distinct from the page-level ones compared above |
| Step 7, Phase 6.4 | No code change. Re-confirm pass per step-7-phases.md's own 6.4 instruction ("Re-confirm 4.6 renders through the normal loaded branch, not through 6.1's error branch or 6.3's empty branch") | Re-checked trail-stop.tsx (unchanged since Phase 4.1): the locked branch is purely a render of the `state` prop, no button/link/onClick anywhere in the file, matching 4.6's original finding exactly, nothing in Phase 5 or 6.2 touched this component. Re-checked trail-detail.tsx's current stop-list render: `trail.stops.length === 0` (the "No stops added to this trail yet." empty branch) and the `<ol>` mapping over TrailStop are mutually exclusive ternary branches, a locked stop can only ever render from inside the non-empty `<ol>` branch, structurally disjoint from the empty one. The entire stop list, locked stops included, sits inside the page-level `{!error && !loading && trail && (...)}` block (line 518), mutually exclusive with the `error` block (510) and `loading` block (512), so a locked stop can never render as, or alongside, either. Also checked Phase 5.3's new completed-state markup (the credential badge and "Trail completed" fallback line) for any accidental reuse of the error/empty styling classes (text-destructive, or the empty state's centered-pill text-muted-foreground treatment) that could blur the concepts -- uses text-foreground, structurally and visually unrelated to both. Nothing to fix, 6.4 remains satisfied after Phase 5 and 6.2's changes exactly as it was at 4.6 |
| Step 7, Phase 6.5 | Fixed one 320px overflow risk: trail-detail.tsx's credential badge (`w-fit` alone, no width cap) could push wider than its container for a long credential_name. Added max-w-full break-words | step-7-phases.md's 6.5: "walk every new screen at a 320px viewport, check for overflow on the trail card row, the stop list, and the credential-earned display. Fix any raw value found rather than a new one invented, token audit." Trail card row (trail-card.tsx) and discover-list.tsx's own sibling row both have no truncate/nowrap on the name span, by design -- flex-col rows with no fixed width, long names wrap rather than overflow, matching the established sibling pattern exactly, nothing to fix there. Stop list (trail-stop.tsx) already has the correct 320px-safe shape: shrink-0 on the number circle and icons, min-w-0 flex-1 on the text column (required for its own truncate to actually take effect on a flex child), unchanged since Phase 4.1. Discovery-content title/body text has no truncate and no fixed width either, wraps normally, correct since that's readable content, not a compact label. Credential-earned display: the badge's `w-fit` (inline-flex, badge.tsx, no width cap of its own) combined with credential_name being a plain `text` column with no length cap (migration 0007, staff-authored via admin-trail-builder.tsx) is a genuine first-of-its-kind risk in this codebase -- the two existing accent-badge usages elsewhere (admin-businesses.tsx, admin-business-detail.tsx) only ever hold fixed short words ("Featured"), not variable-length text. Fixed with max-w-full break-words, both stock utilities already established for exactly this reason in discover-map.tsx's own status pills (same max-w-full break-words combination, same "unpredictable text length inside a compact self-sizing element" problem), not new tokens invented. The "Trail completed" fallback line for a completed route with no credential is a fixed, hardcoded string, never variable-length, so its own w-fit is safe as-is, no change needed there. Token audit: swept every Step 7 file for raw arbitrary-value Tailwind classes (bracket syntax) and inline style objects, found none -- unlike Step 5 Phase 8.5, no raw value needed correcting to a token here |

| Admin sidebar: logo + font sizes | Added the logo (lakbay-pasig-logo.svg, already used by auth-layout.tsx) to admin-sidebar.tsx's header, always visible even when collapsed. Bumped sidebar font sizes one step up the existing type scale throughout | Requested directly, not tied to a build-order phase. Logo: persists collapsed using the exact same pattern the footer's Avatar already established (an h-8 w-8 shrink-0 icon-shaped element with no group-data-[collapsible=icon]:hidden of its own, only the adjacent text hides, group-data-[collapsible=icon]:justify-center re-centers the row). The logo's own viewBox (0 0 512 512, a square squircle mark, confirmed by inspecting the SVG) survives the collapsed 3rem rail (SIDEBAR_WIDTH_ICON, sidebar.tsx) without cropping, and fits the same p-2-padded SidebarHeader/SidebarFooter container at h-8 w-8 exactly as the existing footer avatar already does today, confirmed identical padding on both (grepped SidebarHeader and SidebarFooter's own definitions). Font sizes: per ux-ui-guidelines.md's Consistency Rules ("if a value is used once, it must be reused everywhere that context applies. Never invent a new value for the same purpose" and "Font size hierarchy must be consistent across all pages"), bumped by moving one rung up the type scale using tokens already established elsewhere in the codebase, not new arbitrary sizes: header/footer name text-sm to text-base, footer role text-xs to text-sm. text-base was not yet used anywhere in admin (grepped) but is already a well-established token across the public surface (53 usages), so this is reuse, not invention. Nav item text (sidebarMenuButtonVariants' default size, sidebar.tsx) bumped the same way, text-sm to text-base, with row height nudged h-8 to h-9 to keep proportional room for the larger text; changed at the primitive level rather than per-callsite since admin-sidebar.tsx is the only consumer of SidebarMenuButton anywhere in the codebase (confirmed by repo-wide grep), keeping the "define the token once" principle intact for any future sidebar usage. The sm and lg size variants were left untouched since nothing in the codebase currently exercises them, no basis to guess an appropriate value for a variant with no real usage to check against. Confirmed via twMerge (cn(), utils.ts) that the variant's text-base class correctly wins over the base string's text-sm when both are present in the same cva-generated class string |
| Admin sidebar: logo clipping fix | Fixed the logo overflowing/clipping its own edges during the sidebar collapse/expand transition | Reported directly. Root cause: the logo row's own px-2 py-1 stacked on top of SidebarHeader's own p-2 (both apply regardless of collapsed state), leaving only 16px of the collapsed 3rem rail for a 32px (h-8 w-8) image -- the image was sized larger than the space actually left for it, so it visibly fought the shrinking container during the 200ms width transition instead of settling into it, matching the "gets small and bigger, cut its edges" description. sidebarMenuButtonVariants (sidebar.tsx) already solves this correctly for nav buttons via `group-data-[collapsible=icon]:!size-8 group-data-[collapsible=icon]:!p-2`, collapse-state-aware sizing rather than a static box; the header row had no equivalent. Fixed by adding `group-data-[collapsible=icon]:!p-0` to the row (so SidebarHeader's own p-2 becomes the only inset once collapsed, matching the nav buttons' own collapsed math) and sizing the image down to h-6 w-6 (24px) so it fits the resulting 32px space with margin rather than exactly filling it. Flagged, not changed: the footer's Avatar (h-8 w-8 inside the identical original px-2 py-1-on-p-2 stack) has the same underlying math and is a latent risk of the same bug, just less visible since AvatarFallback renders a circular initial rather than a hard-edged square image -- left untouched since it was not reported as broken and this fix was scoped to what was |

---
